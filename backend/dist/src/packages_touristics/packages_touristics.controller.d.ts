import { PackagesTouristicsService } from './packages_touristics.service';
export declare class PackagesTouristicsController {
    private readonly service;
    constructor(service: PackagesTouristicsService);
}
