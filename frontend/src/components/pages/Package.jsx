import React from 'react';

/**
 * Componente Package (Paquetes).
 * Representa la página donde se muestran los paquetes turísticos o de servicios disponibles.
 * Actualmente, es un componente de marcador de posición con contenido básico.
 */
export const Package = () => {
  return (
    <div>
      <h1>Packages</h1>
      <p>This is the packages page.</p>
    </div>
  );
};