/**
 * Componente SideBar (Barra Lateral).
 * Este componente es un placeholder o una implementación básica de una barra lateral.
 * Actualmente, solo muestra un encabezado simple.
 */
const SideBar = () => {

    return ( 
    <div>
        <h1>hello word</h1>
    </div>
    )
}

export default SideBar